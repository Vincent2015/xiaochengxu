<view class="book-form">
  <form bindsubmit="formSubmit" bindreset="formReset">
    <view class="book-row">
      <label>BookName</label>
      <input id="BookName" name="BookName" placeholder="" value="{{BookName}}" auto-focus/>
    </view>
    <view class="book-row">
      <label>Author</label>
      <input id="Author" name="Author" placeholder="" value="{{Author}}" />
    </view>
    <view class="book-row">
      <label>Price</label>
      <input id="Price" name="Price" placeholder="" value="{{Price}}" />
    </view>
    <view class="book-row">
      <label>Location</label>
      <input id="Location" name="Location" placeholder="" value="{{Location}}" />
    </view>

    <!--<view class="book-row">
   <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
    <view class="picker">
      Location：{{array[index]}}
    </view>
  </picker>
   </view> -->
    <view class="book-row">
      <label>Covers
        <image style="width: 120px; height: 120px; background-color: #eeeeee;" mode="" src="https://mp.weixin.qq.com/debug/wxadoc/dev/image/cat/1.png?t=201754" bindtap="uploadFile"></image>
      </label>
    </view>

    <!--<view class="classname">
      <map id="map" longitude="113.324520" latitude="23.099994" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location style="width: 80%; height: 200px;"></map>
      </view>-->
    <view class="btn-area">
      <button formType="submit">Submit</button>
      <button formType="reset" disabled="{{index}}">Reset</button>
    </view>
  </form>
</view>